<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soon.board.dao.FruitMapper">
	<!-- 이렇게 맞춰 놓으면 밑에서 AS 안 써도 됨 -->
	<!-- <resultMap type="com.soon.board.dto.Fruit" id="userSelectRM">
		<result property="userEmail" column="user_email"/>
		<result property="userPassword" column="user_password"/>
		<result property="userNickname" column="user_nickname"/>
		<result property="userPhoneNumber" column="user_Phone_number"/>
		<result property="userAddress" column="user_address"/>
		<result property="userProfile" column="user_profile"/>
	</resultMap> -->
	
    <select id="getList" resultType="fruit">
        SELECT * FROM user;
    </select>
</mapper>